server {
    listen 8080;
    server_name _;

    location = __BASE_PATH_NOSLASH__ {
        alias /usr/share/nginx/html/index.html;
    }

    location __BASE_PATH__ {
        alias /usr/share/nginx/html/;
        try_files $uri __BASE_PATH__index.html;
    }
}
